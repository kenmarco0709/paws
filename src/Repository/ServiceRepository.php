<?php

namespace App\Repository;

use App\Entity\ServiceEntity;

/**
 * ServiceRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class ServiceRepository extends \Doctrine\ORM\EntityRepository
{
    public function validate($form) {

        $errors = array();

        $action = $form['action'];

        // d = delete
        if($action !== 'd') {

            
            $serviceExist = $this->getEntityManager()->getRepository(ServiceEntity::class)
                ->createQueryBuilder('c')
                ->where('c.id != :id')
                ->andWhere('c.description = :description')
                ->andWhere('c.isDeleted = :is_deleted')
                ->andWhere('c.branch = :branch')
                ->setParameters(array(
                    'id' => $form['id'],
                    'description' => $form['description'],
                    'is_deleted' => false,               
                    'branch' => base64_decode($form['branch'])

                ))
                ->getQuery()->getResult();
            
            if($action != 'u' && $serviceExist){
                $errors[] = 'Service already exist.';
            }

            if(empty($form['description'])){
                $errors[] = 'Description should not be blank.';
            }
        }

        return $errors;
    }

    public function ajax_list(array $get,$userData){

        $columns = array(
            array('st.`description`', 'st.`description`', 'serviceType'),
            array('c.`code`', 'c.`code`'),
            array('c.`description`', 'c.`description`'),
            array('c.`price`', 'c.`price`'),
            array('c.`id`', "c.`id`")
        );
        $asColumns = array();

        
   


        $select = "SELECT";
        $from = "FROM `service` c";
        $joins = " LEFT JOIN `service_type` st ON st.`id` = c.`service_type_id` ";
        $sqlWhere = "WHERE c.`is_deleted` = 0";
        $groupBy = "";
        $orderBy = "";
        $limit = "";
        $stmtParams = array();

        if($userData['type'] != 'Super Admin' || $userData['branchId']){

            $sqlWhere .= " AND c.`branch_id` = :branchId";
            $stmtParams['branchId'] = $userData['branchId'];
        }

        foreach($columns as $key => $column) {
            $select .= ($key > 0 ? ', ' : ' ') . $column[1] . (isset($column[2]) ? ' AS ' . $column[2] : '');
        }

        /*
         * Ordering
         */
        foreach($get['columns'] as $key => $column) {
            if($column['orderable']=='true') {
                if(isSet($get['order'])) {
                    foreach($get['order'] as $order) {
                        if($order['column']==$key) {
                            $orderBy .= (!empty($orderBy) ? ', ' : 'ORDER BY ') . $columns[$key][0] . (!empty($order['dir']) ? ' ' . $order['dir'] : '');
                        }
                    }
                }
            }
        }

        /*
         * Filtering
         */
        if(isset($get['search']) && $get['search']['value'] != ''){
            $aLikes = array();
            foreach($get['columns'] as $key => $column) {
                if($column['searchable']=='true') {
                    $aLikes[] = $columns[$key][0] . ' LIKE :searchValue';
                }
            }
            foreach($asColumns as $asColumn) {
                $aLikes[] = $asColumn . ' LIKE :searchValue';
            }
            if(count($aLikes)) {
                $sqlWhere .= (!empty($sqlWhere) ? ' AND ' : 'WHERE ') . '(' . implode(' OR ', $aLikes) . ')';
                $stmtParams['searchValue'] = "%" . $get['search']['value'] . "%";
            }
        }

        /* Set Limit and Length */
        if(isset( $get['start'] ) && $get['length'] != '-1'){
            $limit = 'LIMIT ' . (int)$get['start'] . ',' . (int)$get['length'];
        }

        $sql = "$select $from $joins $sqlWhere $groupBy $orderBy";
        $query = $this->getEntityManager()->getConnection()->prepare($sql);

        foreach($stmtParams as $k => $v){
            $query->bindValue($k, $v);

        }
        $res = $query->executeQuery();
        $result_count = $res->fetchAllAssociative();


        $sql = "$select $from $joins $sqlWhere $groupBy $orderBy $limit";
        $query = $this->getEntityManager()->getConnection()->prepare($sql);
        foreach($stmtParams as $k => $v){
            $query->bindValue($k, $v);

        }
        $res = $query->executeQuery();
        $result = $res->fetchAllAssociative();

        return array(
            'results' => $result,
            'total' => count($result_count)
        );
    }

    public function autocomplete_suggestions($q, array $userData) {

        $stmtParams = array(
            'q' => "%" . $q['query'] . "%"
        );

        $andWhere = '';

    
        if(isset($q['serviceType'])){
            $andWhere .= ' AND st.`description` = :serviceType';
            $stmtParams['serviceType'] = $q['serviceType'];
        }

               
        if($userData['type'] != 'Super Admin'){

            $andWhere.= ' AND s.`branch_id` = :branchId';
            $stmtParams['branchId'] = $userData['branchId'];
        }

        $sql = "
            SELECT
                s.`id`,
                s.`description` AS data,
                s.`description` AS value,
                s.`price` AS price
            FROM `service` s
            LEFT JOIN  `service_type` st ON st.`id` = s.`service_type_id`
            WHERE s.`description` LIKE :q
            $andWhere
            AND s.`is_deleted` = 0
            ORDER BY s.`description`
            LIMIT 0,20
        ";

        $query = $this->getEntityManager()->getConnection()->prepare($sql);

        foreach($stmtParams as $k => $v){
            $query->bindValue($k, $v);

        }
       
        $res = $query->executeQuery();
        $result = $res->fetchAllAssociative();
        return $result;
    }

    public function getServiceByServiceTypeDesc($desc, $userData) {

       
        $andWhere = ' WHERE st.`description` = "' . $desc . '"';

        if($userData['type'] != 'Super Admin'){

            $andWhere.= ' AND s.`branch_id` = :branchId';
            $stmtParams['branchId'] = $userData['branchId'];
        }

        $sql = "
            SELECT
                s.`id`,
                s.`description`
            FROM `service` s
            LEFT JOIN  `service_type` st ON st.`id` = s.`service_type_id`
            $andWhere
            AND s.`is_deleted` = 0
        ";


        $query = $this->getEntityManager()->getConnection()->prepare($sql);

        foreach($stmtParams as $k => $v){
            $query->bindValue($k, $v);

        }
        
        $res = $query->executeQuery();
        $result = $res->fetchAllAssociative();
        return $result;
    }

    

  


}
